apiVersion: v1
kind: Service
metadata:
  name: timesheet-service
  namespace: chap4-khadijabenjaafar-4nids3
spec:
  type: NodePort
  selector:
    app: timesheet
  ports:
    - port: 8080
      targetPort: 8089
      nodePort: 30007
